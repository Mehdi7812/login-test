import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["randomuser.me"], // اینجا دامین‌هایی که اجازه داری ازشون عکس بیاری
  },
};

export default nextConfig;
